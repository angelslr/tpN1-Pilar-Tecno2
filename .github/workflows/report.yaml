name: 'Test Report'

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 'Install Node'
        uses: actions/setup-node@v2.4.1
      - name: Install dependencies
        run: npm install 
      - name: Install mocha
        run: npm install -g mocha
      - name: "Run Test"
        run: mocha ./test.js